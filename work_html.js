const username = document.getElementById('username');
const neww = createElement('h1');
const text = createTextNode(`${username} Yaxshimisiz)`);
neww.appendChild(text)
const d = document.getElementById('h');
d.appendChild(neww);